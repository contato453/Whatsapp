services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: azvchat
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-azvchat}
      POSTGRES_DB: azvchat
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U azvchat"]
      interval: 5s
      timeout: 5s
      retries: 10
    ports:
      - "5432:5432"

  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    environment:
      DATABASE_URL: postgresql://azvchat:${POSTGRES_PASSWORD:-azvchat}@postgres:5432/azvchat?schema=public
      JWT_SECRET: ${JWT_SECRET:?defina JWT_SECRET no .env}
      WEB_ORIGIN: ${WEB_ORIGIN:-http://localhost:3000}
      API_PORT: 4000
      WHATSAPP_SESSION_DIR: /app/data/sessions
      MEDIA_DIR: /app/data/media
      LOG_LEVEL: ${LOG_LEVEL:-info}
      # Integração com o Azevedo-OS: vazias = desligada (ver .env.example).
      AZEVEDO_OS_API_URL: ${AZEVEDO_OS_API_URL:-}
      AZEVEDO_OS_API_TOKEN: ${AZEVEDO_OS_API_TOKEN:-}
      AZEVEDO_OS_WEB_URL: ${AZEVEDO_OS_WEB_URL:-}
      AZEVEDO_OS_TIMEOUT_MS: ${AZEVEDO_OS_TIMEOUT_MS:-5000}
    volumes:
      # Sessões do WhatsApp e mídias SOBREVIVEM a deploys/restarts.
      - whatsapp_sessions:/app/data/sessions
      - media_store:/app/data/media
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "4000:4000"
    restart: unless-stopped

  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://localhost:4000}
    depends_on:
      - api
    ports:
      - "3000:3000"
    restart: unless-stopped

volumes:
  pgdata:
  whatsapp_sessions:
  media_store:
